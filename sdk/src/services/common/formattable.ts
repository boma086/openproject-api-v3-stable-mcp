// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { FormattableFormat, formattableFormat } from '../activities/models/formattable-format';

/**
 * The shape of the model inside the application code - what the users use
 */
export const formattable = z.lazy(() => {
  return z.object({
    format: formattableFormat,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 *
 * @typedef  {Formattable} formattable
 * @property {FormattableFormat} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type Formattable = z.infer<typeof formattable>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const formattableResponse = z.lazy(() => {
  return z
    .object({
      format: formattableFormat,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const formattableRequest = z.lazy(() => {
  return z
    .object({
      format: formattableFormat,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
