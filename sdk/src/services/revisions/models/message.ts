// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { MessageFormat, messageFormat } from './message-format';

/**
 * The shape of the model inside the application code - what the users use
 */
export const message = z.lazy(() => {
  return z.object({
    format: messageFormat,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 * The commit message of the revision
 * @typedef  {Message} message - The commit message of the revision - The commit message of the revision
 * @property {MessageFormat} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type Message = z.infer<typeof message>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const messageResponse = z.lazy(() => {
  return z
    .object({
      format: messageFormat,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const messageRequest = z.lazy(() => {
  return z
    .object({
      format: messageFormat,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
