// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { NotificationMessageFormat, notificationMessageFormat } from './notification-message-format';

/**
 * The shape of the model inside the application code - what the users use
 */
export const notificationMessage = z.lazy(() => {
  return z.object({
    format: notificationMessageFormat,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 * A customised notification message, which will overwrite the standard notification.
 * @typedef  {NotificationMessage} notificationMessage - A customised notification message, which will overwrite the standard notification. - A customised notification message, which will overwrite the standard notification.
 * @property {NotificationMessageFormat} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type NotificationMessage = z.infer<typeof notificationMessage>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const notificationMessageResponse = z.lazy(() => {
  return z
    .object({
      format: notificationMessageFormat,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const notificationMessageRequest = z.lazy(() => {
  return z
    .object({
      format: notificationMessageFormat,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
