// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { ProjectModel, projectModel, projectModelRequest, projectModelResponse } from '../../common/project-model';

/**
 * The shape of the model inside the application code - what the users use
 */
export const projectCollectionModelEmbedded = z.lazy(() => {
  return z.object({
    elements: z.array(projectModel),
  });
});

/**
 *
 * @typedef  {ProjectCollectionModelEmbedded} projectCollectionModelEmbedded
 * @property {ProjectModel[]}
 */
export type ProjectCollectionModelEmbedded = z.infer<typeof projectCollectionModelEmbedded>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const projectCollectionModelEmbeddedResponse = z.lazy(() => {
  return z
    .object({
      elements: z.array(projectModelResponse),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const projectCollectionModelEmbeddedRequest = z.lazy(() => {
  return z
    .object({
      elements: z.array(projectModelRequest),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});
