// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import {
  MembershipReadModel,
  membershipReadModel,
  membershipReadModelRequest,
  membershipReadModelResponse,
} from './membership-read-model';

/**
 * The shape of the model inside the application code - what the users use
 */
export const membershipCollectionModelEmbedded = z.lazy(() => {
  return z.object({
    elements: z.array(membershipReadModel),
  });
});

/**
 *
 * @typedef  {MembershipCollectionModelEmbedded} membershipCollectionModelEmbedded
 * @property {MembershipReadModel[]}
 */
export type MembershipCollectionModelEmbedded = z.infer<typeof membershipCollectionModelEmbedded>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const membershipCollectionModelEmbeddedResponse = z.lazy(() => {
  return z
    .object({
      elements: z.array(membershipReadModelResponse),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const membershipCollectionModelEmbeddedRequest = z.lazy(() => {
  return z
    .object({
      elements: z.array(membershipReadModelRequest),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});
