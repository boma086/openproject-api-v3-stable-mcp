// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const _embeddedDetails = z.lazy(() => {
  return z.object({
    attribute: z.string().optional(),
  });
});

/**
 *
 * @typedef  {_EmbeddedDetails} _embeddedDetails
 * @property {string}
 */
export type _EmbeddedDetails = z.infer<typeof _embeddedDetails>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const _embeddedDetailsResponse = z.lazy(() => {
  return z
    .object({
      attribute: z.string().optional(),
    })
    .transform((data) => ({
      attribute: data['attribute'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const _embeddedDetailsRequest = z.lazy(() => {
  return z
    .object({
      attribute: z.string().optional(),
    })
    .transform((data) => ({
      attribute: data['attribute'],
    }));
});
