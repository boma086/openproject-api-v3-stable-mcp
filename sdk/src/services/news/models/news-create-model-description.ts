// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { DescriptionFormat6, descriptionFormat6 } from './description-format-6';

/**
 * The shape of the model inside the application code - what the users use
 */
export const newsCreateModelDescription = z.lazy(() => {
  return z.object({
    format: descriptionFormat6,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 * The main body of the news with all the details
 * @typedef  {NewsCreateModelDescription} newsCreateModelDescription - The main body of the news with all the details - The main body of the news with all the details
 * @property {DescriptionFormat6} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type NewsCreateModelDescription = z.infer<typeof newsCreateModelDescription>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const newsCreateModelDescriptionResponse = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat6,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const newsCreateModelDescriptionRequest = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat6,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
