// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { DescriptionFormat5, descriptionFormat5 } from './description-format-5';

/**
 * The shape of the model inside the application code - what the users use
 */
export const workPackageModelDescription = z.lazy(() => {
  return z.object({
    format: descriptionFormat5,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 * The work package description
 * @typedef  {WorkPackageModelDescription} workPackageModelDescription - The work package description - The work package description
 * @property {DescriptionFormat5} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type WorkPackageModelDescription = z.infer<typeof workPackageModelDescription>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const workPackageModelDescriptionResponse = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat5,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const workPackageModelDescriptionRequest = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat5,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
