// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const querySortByModel = z.lazy(() => {
  return z.object({
    id: z.string(),
    name: z.string(),
  });
});

/**
 *
 * @typedef  {QuerySortByModel} querySortByModel
 * @property {string} - QuerySortBy id
 * @property {string} - QuerySortBy name
 */
export type QuerySortByModel = z.infer<typeof querySortByModel>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const querySortByModelResponse = z.lazy(() => {
  return z
    .object({
      id: z.string(),
      name: z.string(),
    })
    .transform((data) => ({
      id: data['id'],
      name: data['name'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const querySortByModelRequest = z.lazy(() => {
  return z
    .object({
      id: z.string(),
      name: z.string(),
    })
    .transform((data) => ({
      id: data['id'],
      name: data['name'],
    }));
});
