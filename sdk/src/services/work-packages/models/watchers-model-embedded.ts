// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import {
  _EmbeddedElements8,
  _embeddedElements8,
  _embeddedElements8Request,
  _embeddedElements8Response,
} from './_embedded-elements-8';

/**
 * The shape of the model inside the application code - what the users use
 */
export const watchersModelEmbedded = z.lazy(() => {
  return z.object({
    elements: z.array(_embeddedElements8).optional(),
  });
});

/**
 *
 * @typedef  {WatchersModelEmbedded} watchersModelEmbedded
 * @property {_EmbeddedElements8[]}
 */
export type WatchersModelEmbedded = z.infer<typeof watchersModelEmbedded>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const watchersModelEmbeddedResponse = z.lazy(() => {
  return z
    .object({
      elements: z.array(_embeddedElements8Response).optional(),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const watchersModelEmbeddedRequest = z.lazy(() => {
  return z
    .object({
      elements: z.array(_embeddedElements8Request).optional(),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});
