// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { _LinksSelf13, _linksSelf13, _linksSelf13Request, _linksSelf13Response } from './_links-self-13';
import {
  ExecuteImmediately,
  executeImmediately,
  executeImmediatelyRequest,
  executeImmediatelyResponse,
} from './execute-immediately';

/**
 * The shape of the model inside the application code - what the users use
 */
export const customActionModelLinks = z.lazy(() => {
  return z.object({
    self: _linksSelf13,
    executeImmediately: executeImmediately,
  });
});

/**
 * 
 * @typedef  {CustomActionModelLinks} customActionModelLinks   
 * @property {_LinksSelf13} - This custom action

``Resource``: CustomAction
 * @property {ExecuteImmediately} - Execute this custom action.
 */
export type CustomActionModelLinks = z.infer<typeof customActionModelLinks>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const customActionModelLinksResponse = z.lazy(() => {
  return z
    .object({
      self: _linksSelf13Response,
      executeImmediately: executeImmediatelyResponse,
    })
    .transform((data) => ({
      self: data['self'],
      executeImmediately: data['executeImmediately'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const customActionModelLinksRequest = z.lazy(() => {
  return z
    .object({
      self: _linksSelf13Request,
      executeImmediately: executeImmediatelyRequest,
    })
    .transform((data) => ({
      self: data['self'],
      executeImmediately: data['executeImmediately'],
    }));
});
