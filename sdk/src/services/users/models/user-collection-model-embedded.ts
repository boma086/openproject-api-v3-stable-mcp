// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { UserModel, userModel, userModelRequest, userModelResponse } from '../../common/user-model';

/**
 * The shape of the model inside the application code - what the users use
 */
export const userCollectionModelEmbedded = z.lazy(() => {
  return z.object({
    elements: z.array(userModel),
  });
});

/**
 *
 * @typedef  {UserCollectionModelEmbedded} userCollectionModelEmbedded
 * @property {UserModel[]}
 */
export type UserCollectionModelEmbedded = z.infer<typeof userCollectionModelEmbedded>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const userCollectionModelEmbeddedResponse = z.lazy(() => {
  return z
    .object({
      elements: z.array(userModelResponse),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const userCollectionModelEmbeddedRequest = z.lazy(() => {
  return z
    .object({
      elements: z.array(userModelRequest),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});
