// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const activityCommentWriteModel = z.lazy(() => {
  return z.object({
    comment: z.string().optional(),
  });
});

/**
 *
 * @typedef  {ActivityCommentWriteModel} activityCommentWriteModel
 * @property {string}
 */
export type ActivityCommentWriteModel = z.infer<typeof activityCommentWriteModel>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const activityCommentWriteModelResponse = z.lazy(() => {
  return z
    .object({
      comment: z.string().optional(),
    })
    .transform((data) => ({
      comment: data['comment'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const activityCommentWriteModelRequest = z.lazy(() => {
  return z
    .object({
      comment: z.string().optional(),
    })
    .transform((data) => ({
      comment: data['comment'],
    }));
});
