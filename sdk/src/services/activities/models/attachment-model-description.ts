// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { DescriptionFormat4, descriptionFormat4 } from './description-format-4';

/**
 * The shape of the model inside the application code - what the users use
 */
export const attachmentModelDescription = z.lazy(() => {
  return z.object({
    format: descriptionFormat4,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 * A user provided description of the file
 * @typedef  {AttachmentModelDescription} attachmentModelDescription - A user provided description of the file - A user provided description of the file
 * @property {DescriptionFormat4} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type AttachmentModelDescription = z.infer<typeof attachmentModelDescription>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const attachmentModelDescriptionResponse = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat4,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const attachmentModelDescriptionRequest = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat4,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
