// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { DescriptionFormat3, descriptionFormat3 } from './description-format-3';

/**
 * The shape of the model inside the application code - what the users use
 */
export const elementsDescription2 = z.lazy(() => {
  return z.object({
    format: descriptionFormat3,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 * A user provided description of the file
 * @typedef  {ElementsDescription2} elementsDescription2 - A user provided description of the file - A user provided description of the file
 * @property {DescriptionFormat3} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type ElementsDescription2 = z.infer<typeof elementsDescription2>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const elementsDescription2Response = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat3,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const elementsDescription2Request = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat3,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
