// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import { DescriptionFormat10, descriptionFormat10 } from './description-format-10';

/**
 * The shape of the model inside the application code - what the users use
 */
export const versionModelDescription = z.lazy(() => {
  return z.object({
    format: descriptionFormat10,
    raw: z.string().optional(),
    html: z.string().optional(),
  });
});

/**
 *
 * @typedef  {VersionModelDescription} versionModelDescription
 * @property {DescriptionFormat10} - Indicates the formatting language of the raw text
 * @property {string} - The raw text, as entered by the user
 * @property {string} - The text converted to HTML according to the format
 */
export type VersionModelDescription = z.infer<typeof versionModelDescription>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const versionModelDescriptionResponse = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat10,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const versionModelDescriptionRequest = z.lazy(() => {
  return z
    .object({
      format: descriptionFormat10,
      raw: z.string().optional(),
      html: z.string().optional(),
    })
    .transform((data) => ({
      format: data['format'],
      raw: data['raw'],
      html: data['html'],
    }));
});
