// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import {
  _EmbeddedElements10,
  _embeddedElements10,
  _embeddedElements10Request,
  _embeddedElements10Response,
} from './_embedded-elements-10';

/**
 * The shape of the model inside the application code - what the users use
 */
export const versionsByProjectModelEmbedded = z.lazy(() => {
  return z.object({
    elements: z.array(_embeddedElements10).optional(),
  });
});

/**
 *
 * @typedef  {VersionsByProjectModelEmbedded} versionsByProjectModelEmbedded
 * @property {_EmbeddedElements10[]}
 */
export type VersionsByProjectModelEmbedded = z.infer<typeof versionsByProjectModelEmbedded>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const versionsByProjectModelEmbeddedResponse = z.lazy(() => {
  return z
    .object({
      elements: z.array(_embeddedElements10Response).optional(),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const versionsByProjectModelEmbeddedRequest = z.lazy(() => {
  return z
    .object({
      elements: z.array(_embeddedElements10Request).optional(),
    })
    .transform((data) => ({
      elements: data['elements'],
    }));
});
