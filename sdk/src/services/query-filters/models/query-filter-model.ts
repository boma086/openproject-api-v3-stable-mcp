// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';

/**
 * The shape of the model inside the application code - what the users use
 */
export const queryFilterModel = z.lazy(() => {
  return z.object({
    id: z.string(),
  });
});

/**
 *
 * @typedef  {QueryFilterModel} queryFilterModel
 * @property {string} - QueryFilter id
 */
export type QueryFilterModel = z.infer<typeof queryFilterModel>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const queryFilterModelResponse = z.lazy(() => {
  return z
    .object({
      id: z.string(),
    })
    .transform((data) => ({
      id: data['id'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const queryFilterModelRequest = z.lazy(() => {
  return z
    .object({
      id: z.string(),
    })
    .transform((data) => ({
      id: data['id'],
    }));
});
