// This file was generated by liblab | https://liblab.com/

import { z } from 'zod';
import {
  BudgetModelLinks,
  budgetModelLinks,
  budgetModelLinksRequest,
  budgetModelLinksResponse,
} from './budget-model-links';

/**
 * The shape of the model inside the application code - what the users use
 */
export const budgetModel = z.lazy(() => {
  return z.object({
    _links: budgetModelLinks.optional(),
  });
});

/**
 *
 * @typedef  {BudgetModel} budgetModel
 * @property {BudgetModelLinks}
 */
export type BudgetModel = z.infer<typeof budgetModel>;

/**
 * The shape of the model mapping from the api schema into the application shape.
 * Is equal to application shape if all property names match the api schema
 */
export const budgetModelResponse = z.lazy(() => {
  return z
    .object({
      _links: budgetModelLinksResponse.optional(),
    })
    .transform((data) => ({
      _links: data['_links'],
    }));
});

/**
 * The shape of the model mapping from the application shape into the api schema.
 * Is equal to application shape if all property names match the api schema
 */
export const budgetModelRequest = z.lazy(() => {
  return z
    .object({
      _links: budgetModelLinksRequest.optional(),
    })
    .transform((data) => ({
      _links: data['_links'],
    }));
});
